[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt"
]

[phases.build]
cmds = []

# Note: Start command is set in Railway Settings → Deploy
# This prevents mise from trying to install Python

